
Task
Apartment area
There is a plan of an apartment consisting of 4 rooms. The premises are square in shape, and accordingly the sizes: a, b, c, d.
 
Write a function called square that takes four numbers a, b, c, d as arguments and returns the total area of the apartment.
Examples:
• function square (1, 2, 4, 8) should return 85;
• function square (1, 3, 8, 10) should return 174.

Solution
function square (a, b, c, d) {
return Math.pow (a, 2) + Math.pow (b, 2) + Math.pow (c, 2) + Math.pow (d, 2);
}
