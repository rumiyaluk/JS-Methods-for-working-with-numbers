Task
Random integer from 0 to 10
Math.random () returns a random number between 0 (inclusive) and 1 (not inclusive).
Write a function named integerFrom0To10 that returns a random integer between 0 and 10, inclusive.
Examples:
• the integerFrom0To10 () function returns 10 (any integer from 0 to 10);
• the integerFrom0To10 () function returns 0 (any integer from 0 to 10);
• integerFrom0To10 () function returns 9 (any integer from 0 to 10).

Solution
function integerFrom0To10 () {
 return Math.floor (Math.random () * 10);
}
