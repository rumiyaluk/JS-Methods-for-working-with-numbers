Task
Random integer from 1 to 10
Math.random () returns a random number between 0 (inclusive) and 1 (not inclusive).
Write a function named integerFrom1To10 that returns a random integer between 1 and 10, inclusive.
Examples:
• the integerFrom1To10 () function returns 10 (any integer from 1 to 10);
• the integerFrom1To10 () function returns 1 (any integer from 1 to 10);
• the integerFrom1To10 () function returns 8 (any integer from 1 to 10).

Solution
function integerFrom1To10 () {
 return Math.floor (Math.random () * 10 + 1);
}
