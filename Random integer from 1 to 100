Task
Random integer from 1 to 100
Math.random () returns a random number between 0 (inclusive) and 1 (not inclusive).
Write a function named integerFrom1To100 that returns a random integer between 1 and 100, inclusive.
Examples:
• the integerFrom1To100 () function returns 100 (any integer from 1 to 100);
• the integerFrom1To100 () function returns 1 (any integer from 1 to 100);
• the integerFrom1To100 () function returns 67 (any integer from 1 to 100).

Solution
function integerFrom1To100 () {
 return Math.floor (Math.random () * 100 + 1);
}
