Task
Random integer three-digit number
Math.random () returns a random number between 0 (inclusive) and 1 (not inclusive).
Write a function named integerFrom100To999 that returns a random three-digit integer (that is, a number between 100 and 999, inclusive).
Examples:
• the integerFrom100To999 () function returns 100 (any integer from 100 to 999);
• the integerFrom100To999 () function returns 990 (any integer from 100 to 999);
• the integerFrom100To999 () function returns 744 (any integer from 100 to 999).

Solution
function integerFrom100To999 () {
 let min = 100;
 let max = 999;
   return Math.floor (Math.random () * (max - min + 1) + min);
}
