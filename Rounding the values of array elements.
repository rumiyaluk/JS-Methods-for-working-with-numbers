
Task
Rounding the values of array elements.
Write a function roundElementsOfArray that takes an array arr as an argument and returns an array in which all elements
are rounded to the nearest integer.
Examples:
roundElementsOfArray ([10.23, 34.89, 0.1, 4.45, 8.5]) should return [10, 35, 0, 4, 9]

Solution
function roundElementsOfArray(arr){
  let x = 0;
  let len = arr.length
  while(x < len){ 
    arr[x] = Math.round(arr[x]); 
    x++
  }
return  arr;
}
